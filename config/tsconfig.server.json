{
    "extends": "../tsconfig",
    "compilerOptions": {
        // Supported by Node.js 10.16.3 (with the exception of tail recursion)
        "target": "es2018"
    },
    "files": [
        "../src/server.ts"
    ]
}
