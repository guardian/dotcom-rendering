name: DCR Deployment
on:
  push:
    paths-ignore:
      - "apps-rendering/**"
jobs:

  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        run: echo "Coming soon..."



