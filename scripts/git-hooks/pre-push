#!/usr/bin/env node

const execa = require('execa')

const typeScriptBuild = () =>
    execa(
        'tsc',
        [],
        {
            stdio: 'inherit',
        }
    );

typeScriptBuild();


